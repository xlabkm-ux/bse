# AGENTS.md

<INSTRUCTIONS>
Используй PowerShell (нет rg).

Unity находится здесь:
`C:\Program Files\Unity\Hub\Editor\6000.4.3f1\Editor\Unity.exe`

План разработки хранится в:
`Docs/development_plan_sessions.md`

В новых чатах сначала сверяйся с этим файлом, смотри раздел
`Active Continuation` и продолжай следующую незавершенную Session именно из
активного трека, если пользователь не указал другую задачу.

Если задача относится к подготовке локального pilot testing readiness или к
`Pilot Testing Preparation for Breach Scenario Engine`, сначала смотри
`Docs/pilot_testing_preparation_plan.md` и его раздел
`Summary Execution Plan by Codex Sessions`, затем продолжай следующую
незавершенную Session именно из этого плана.

Текущий активный трек:
`BREACH Technology Transfer`.

Перед началом работы по сессии прочитай Anchor Files из раздела
`Handoff Checklist` в плане разработки.

Перед любыми Unity-зависимыми проверками сначала сверяй, что Google-пользователь
в Codex и Google-пользователь в Unity Hub совпадают. Если аккаунты не совпадают,
сначала попроси разработчика прелогиниться в Unity Hub под Codex-пользователем.

После успешного выполнения задания, прохождения релевантных проверок и создания commit сразу выполняй `git push` в текущую ветку. Не добавляй посторонние изменения пользователя и не выполняй force-push без отдельного явного запроса.
</INSTRUCTIONS>
